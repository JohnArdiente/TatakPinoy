@model IEnumerable<TatakPinoy.Models.ConsigneeStatus>

@{
    ViewData["Title"] = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<div class="container">

    <h4>Create Consignee Status</h4>

    <p>
        <a asp-action="Create" class="btn btn-outline-success">Create New</a>
    </p>
    <table class="table">
        <thead>
            <tr>
                <th>
                   Consignee Status Description
                </th>
                <th></th>
            </tr>
        </thead>
        <tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>
                        @Html.DisplayFor(modelItem => item.ConsigneeStatusDesc) 
                    </td>
                    <td>
                        @*<a asp-action="Edit" class="btn btn-outline-primary" asp-route-id="@item.ConsigneeStatusId">Edit</a>

                        <a asp-action="Delete" class="btn btn-outline-danger" asp-route-id="@item.ConsigneeStatusId">Delete</a>*@
                    </td>
                </tr>
            }
        </tbody>
    </table>
</div>